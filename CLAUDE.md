# CLAUDE.md

YouTube Shorts Maker — 세로형 쇼츠 자동 생성 엔진 + 편집 앱.
기획과 확정 결정은 `docs/PRD.md`와 `docs/types/quiz.md`, 작업 규칙은 `CONTRIBUTING.md`,
화면 수치는 `docs/design/d1-video-design-spec.md`(영상)·`d2-app-design-spec.md`(앱)에 있다.
**아래는 그 문서들을 읽어서는 알 수 없거나, 읽으면 오해하게 되는 것만 적는다.**

## 작업 방식

모든 작업은 GitHub 이슈 단위다. 착수 전 `CONTRIBUTING.md`를 읽는다 — 라벨 체계,
리파인먼트, 브랜치·PR 규칙이 거기 있다. 이슈 본문의 완료 조건이 완료 정의다.
새 결정을 내렸으면 코드만 바꾸지 말고 PRD 14장(또는 퀴즈 스펙 9장)을 갱신한다.

## 로드맵

**Phase 0~4 완료.** CLI 한 번에 `config.used.yaml` · `quiz.json` · `scenes.json` ·
`metadata.json` · `audio/seg-*.mp3` · `voice.mp3` · `captions.srt` · `project.json` ·
`final_short.mp4`가 나오고(`--text-file`·`--url` 경로는 `source.json`도), 앱은 열기·저장·장면
목록·프리뷰·문제 편집·속성 편집·최종 렌더·재생성까지 한다. **지금은 Phase 5다** — 범용 입력
경로(#94·#95)가 붙어 세 갈래가 모두 살고, 링크 입력의 출처가 `metadata.json`에 남고(#100),
`--resume`으로 실패한 run을 이어 돌린다(#36). 이미지·영상 배경에 zoom/pan이 걸리고(#34, 기본은
`none` — 앱에 끄는 칸이 없다), 배경음악과 낭독 구간 ducking이 붙었다(#35, 기본은 음악 없음 —
**앱에 고르는 칸이 없어 경로는 config 하나다**). 남은 것은 **#32와 #37 둘뿐이다.**

- **#32(`blocked`)는 선행이 없어 막혀 있다** — 요약·대본을 읽는 쪽이 없다. 원문을 소비하는
  쇼츠 타입 이슈가 먼저 생겨야 하고(저장소에 없다) `summary.json`·`script.txt`의 모양은 그쪽이
  정한다. 소비자 없이 두 모듈을 먼저 만드는 것은 PRD 12가 피한 배치다.
- **#37(`needs-refinement`)이 다음에 다듬을 이슈다** — 업로드 자동화 검토는 PRD 12의 마지막
  항목이고 3장이 MVP 비목표로 둔 것이라, 스파이크 결론이 곧 구현 착수를 뜻하지 않는다.
- **#33은 닫혔다**(`not planned`). ASS 자막과 word-level timestamp는 리파인먼트에서 "할 일이
  남지 않는다"로 끝났다 — 번인이 그 목적을 이미 채웠고(정답 확대는 `drawtext`의 `fontsize`
  시간 표현식, 단어 경계는 edge-tts provider가 준다), ASS로 옮기면 D1 확정 스펙을 두 곳에서
  구현하게 되며 libass가 사용자 FFmpeg 빌드에 있다고 가정할 수 없다. **다시 여는 조건은 단어
  단위 강조를 요구하는 디자인 결정이나 ASS를 실제로 적용하는 소비자이고, 그때는 새 이슈다.**

| Phase | 이슈 | 내용 |
| --- | --- | --- |
| 0 | #1–#8 | provider 결정, CLI·config·스키마·타입 플러그인 골격 |
| 1 | #48, #9–#13 | LLM provider 구현, 퀴즈 텍스트 산출물 |
| 2 | #14–#18 | 오디오와 자막 |
| 3 | #55, #56, #38, #19–#24 | 렌더링 |
| 4 | #25–#30, #92, #77, #79–#83 | 편집 앱 |
| 5 | #31–#37, #94, #95, #100 | 범용 입력 경로, 품질 개선, 업로드 검토 |

- **#38 이후 번호는 Phase 순서를 따르지 않는다** — 소속은 위 표를 본다.
- **#29·#31은 구현 이슈가 아니라 추적 이슈다.** #29 → #79~#83, #31 → #94·#95·#100으로
  갈라졌다 — 닫힌 추적 이슈를 가리키는 코드 주석이 남아 있으면 그 자리를 이은 번호가 위다.
- **초안의 마일스톤 순서(앱 골격을 렌더링보다 앞에 두는 배치)로 되돌리지 말 것.** 편집 대상도
  렌더 결과도 없는 상태에서 에디터를 만들면 재작업이 발생한다 — 위 순서는 수직 슬라이스를 먼저
  완성하고 앱을 붙인다 (PRD 12장).

## 문서를 그대로 읽으면 틀리는 지점

- **정답 검증은 블라인드 방식이다.** 퀴즈 스펙 5장의 "2차 LLM 검증"을 생성된 정답을 보여주고
  "맞습니까?"라고 묻는 식으로 구현하면 같은 모델이 자기 출력에 동조해 효과가 없다. 정답을
  감추고 질문만 다시 물어 독립 답변을 받아 비교한다. **실검출이 나오는 경로는 모호성
  프로브다** — 블라인드 재답변은 *정답이 틀린 게 아니라 질문이 모호한* 유형을 원리상 잡지
  못한다(재답변 모델도 같은 근거로 같은 답을 낸다). `quiz_verifier.py` (#10)
- **`verify.status = "verified"`는 "정답이 맞다"가 아니라 "검증 단계가 결함을 찾지
  못했다"이고, `unverified`는 성공이 아니라 "검증이 완료되지 않았다"이다.** 임계값을 적용해
  `verified`를 `flagged`로 내리는 것은 검증기가 아니라 `quiz_review.py`다. (#10, #11)
- **flagged는 실행을 멈추지 않는다.** 중단으로 구현하면 검수 주체인 사람에게서 검수할
  산출물을 빼앗는다. 기본은 경고 후 진행에 종료 코드 0이고, 멈추는 것은 `--fail-on-flagged`를
  지정한 쪽의 선택이다. (#11)
- **검수 훅은 세 번째 플러그인 축이 아니다.** `ShortsType.content_review`는 `config_check`와
  같은 성격의 훅이고, 교체 가능한 축은 생성기·장면 템플릿 둘뿐이다. 파이프라인이 받는
  `ContentIssue`에 `confidence` 같은 칸을 열면 공통 코드가 타입별 판정 기준을 알게 된다 —
  어느 부분 / 무엇 / 왜 세 문자열로 끝낸다. (#11)
- **duration은 실측 오디오 길이 + 패딩으로 확정한다.** 스펙의 초 단위 값은 목표치이고,
  낭독이 없는 장면(`countdown`·`hook`·`cta`)만 고정 길이다. (PRD 7.5.1)
- **`TTSProvider` 계약에 길이가 없다.** 텍스트 하나 → 오디오 파일 하나가 전부이고 실측은
  공통 코드(`tts/speech.py`)가 `ffprobe`로 한다. 캐시도 같은 층에 있고 **run 디렉터리
  밖**(`.cache/tts`)이다 — run마다 새 디렉터리라 안에 두면 반복 실행이 매번 전부 재합성한다.
  단어 타이밍의 `None`(기능 없음)과 빈 목록(이번엔 없음)은 다른 값이다. (#14)
- **세그먼트 재사용은 캐시와 다른 층이다.** `audio/segments.json`은 **같은 run 디렉터리
  안에서** 사람이 교체한 세그먼트를 지키므로, 기록된 길이를 믿지 않고 파일을 다시 잰다
  (`SpeechSynthesizer.measure` — 측정 경계가 하나여야 한다). (#15)
- **`captions.max_chars_per_line`은 번인 줄바꿈이 아니다.** 번인은 `drawtext` 요소마다 폰트
  크기가 달라 렌더러가 `floor(840 / font_size)`로 계산한다. 그 숫자를 config 키에 옮겨 적으면
  SRT와 번인이 한 값을 공유하게 되어 어느 쪽도 맞지 않는다. (#17, #20)
- **run 디렉터리를 다시 여는 코드는 `load_config()`를 인자 없이 부르면 안 된다** — 그 호출은
  **cwd**의 `config.yaml`을 찾는데 앱 백엔드는 cwd를 앱이 정한다. 자리는
  `config.load_run_config(run_dir)` 하나이고, 그것이 읽는 `config.used.yaml`은 산출물 중
  유일하게 JSON이 아니며 `serialize_artifact`를 지나지 않는다. (#92)
- **공통 파이프라인은 `scenes.json`만 읽는다.** 이 경계는 `tests/test_type_boundary.py`가
  강제한다 — `types/` 밖에서 타입 패키지를 import하거나 `load_quiz`를 쓰거나 `quiz.json`
  문자열을 들면 깨진다. 타입 전용 정보가 렌더에 필요하면 장면 템플릿이 `scenes.json` 필드로
  옮겨 담는다. (#8)
- **산출물 JSON의 필드명·열거값은 `src/shorts_maker/schemas/`가 확정한다** — 문서는 그 이름을
  설명하는 쪽이다. `scenes.json`은 스키마 하나 + 확정 검증(`validate_scenes_final`) 구조다:
  오디오 필드가 없는 초안도 통과해야 하기 때문이다. (#7)
- **타입 선언은 타입 패키지가 소유한다.** `shorts_types.py`는 이름 → 선언 모듈 경로만 알고,
  `scenes.json`·`project.json`의 `type` 후보도 검증 시점에 레지스트리를 조회한다 — 스키마에
  타입 이름을 추가하지 말 것. 타입별 설정 제약(퀴즈의 문제 수 3~5 등)도 `config.SPEC`이 아니라
  `SHORTS_TYPE.config_check`에 있고, run 디렉터리를 만들기 전에 걸린다. (#8, #9)
- **`--json-schema`로 넘기는 JSON Schema는 산출물 스키마에서 파생한다**
  (`Rule.to_json_schema()` / `Object.without()`). 프롬프트 쪽에 필드 이름을 손으로 적으면
  계약이 두 곳에 생긴다. 검증은 여전히 `Rule.check`가 한다 — 파생 결과로 검증하지 말 것. (#9)
- **산출물은 타입·입력 경로에 따라 다르다.** `script.txt`·`summary.json`·`source.json`은
  퀴즈 + `--topic` 경로에서 생성되지 않고, 없는 것이 실패가 아니다. (PRD 6.2)
- **본문 추출기 import는 함수 안에 있어야 한다** (`source.load_extractor`). trafilatura는
  `source` extra라 없을 수 있고, 있어도 `import`가 810ms다 — 모듈 최상단으로 옮기면 없는
  환경에서 `--topic`이 죽고 있는 환경에서는 모든 실행이 그 시간을 낸다. **`dev` extra에도
  들어 있는 이유는 추출 품질이 테스트 대상이기 때문이다** — 대역으로 바꾸면 동어반복이 된다.
- **링크 거부는 세 신호뿐이다** — HTTP 상태 · `Content-Type` · 본문 길이. "유료인가 로그인
  벽인가"를 판별하는 코드를 더하면 정상 기사를 함께 버린다(로그인 안내문 800자 > 정상 단신
  735자, 스파이크 #31 3장). 임계값의 목적은 안내문 걸러내기가 아니라 **빈 본문 걸러내기**다.
- **`source.json`의 `url`은 사용자가 친 주소가 아니라 리다이렉트가 도착한 곳이다.** 단축
  링크를 넣고 값이 다르다고 버그로 고치면 그 칸이 출처를 가리키지 않게 된다. (#95)
- **최종 mp4는 임시 파일에 렌더하고 바꿔 끼운다** (`video_renderer.render` → `build_command`의
  `destination`). **이어 돌리기(#36)의 판단이 이것에 의존한다** — 잘린 mp4가 남으면 "산출물이
  있으니 건너뛴다"가 그 파일을 최종 결과로 넘긴다. 함정은 테스트 대역이다: **목적지 파일을
  만들지 않는 가짜 ffmpeg는 이제 성공 경로에서 `commit_staged`가 `FileNotFoundError`로 터진다**
  (원인이 렌더처럼 보이지만 대역이다).
- **이어 돌리기는 타입을 모른다.** 장면이 `scenes.json`에서 오므로 콘텐츠 파일명도 타입 선언도
  필요하지 않다 — 재생성(#77)과 갈리는 지점이 그것이다. 요구 산출물 셋(`config.used.yaml`·
  `scenes.json`·`metadata.json`)은 **모델이 만드는 것**이라 없으면 멈춘다.
- **`resume`은 `project.json`이 있으면 다시 만들지 않는다.** 사람이 앱에서 얹은
  `render.scene_overrides`·`review`가 거기 살아서다. 자막은 반대로 타임라인을 다시 확정하면
  파일이 있어도 다시 만든다(타임코드가 낡는다) — 두 규칙이 갈리는 것이 의도다.
- **`--force segments`가 provider 호출을 늘리지 않는다.** TTS 캐시가 run 디렉터리 밖이라
  같은 문장은 복사된다 — `Report.synthesized`(그대로 쓰지 않은 세그먼트 수)와 provider 호출
  수를 같은 값으로 보는 테스트는 거짓으로 실패한다.
- **생성 전용 CLI 인자의 기본값은 파서에 없다** (`--type`·`--out`·`--fail-on-flagged`가
  `argparse.SUPPRESS`, 채우는 자리는 `main._fill_generation_defaults`). 기본값을 파서로 되돌리면
  "주지 않았다"와 "기본값을 줬다"가 갈리지 않아 **`--resume`과 함께 온 인자를 거부하는 규칙이
  조용히 죽는다.** `--help`의 기본값 문구도 그래서 손으로 적혀 있다.
- **전 구간 스모크(`tests/test_e2e_smoke.py`)는 `conftest`의 `stub_tts`·`stub_ffmpeg`를 쓰지
  않는다.** `b"stub-audio"`는 진짜 FFmpeg가 디코드하지 못하고, `stub_ffmpeg`는 `ffprobe`까지
  가짜 길이로 답해 duration 확정이 실측 경로를 지나지 않는다. **파이프라인 실행은 모듈 스코프
  픽스처의 3회뿐이다** — 테스트마다 돌리면 1080x1920 인코딩이 그만큼 붙는다. (#24)

## 디자인 시안

**시안이 아니라 확정 스펙이 구현 기준이다.** 시안을 직접 열면 틀린다 — 세 배치 전부 저장소의
확정값을 잘못 알고 있던 지점이 있었고, 그 대조 결과가 각 스펙의 "시안에서 고친 것" 절이다.
**더 받을 배치는 없다.**

조용히 깨지는 것 둘:

- **CSS `-webkit-text-stroke` 값을 `borderw`에 그대로 넣으면 두 배로 두꺼워진다.** CSS stroke는
  글리프 경로 중심 기준이라 절반만 보이고 `borderw`는 전부 바깥이다. 이 디자인은 외곽선이
  지배적 요소라 이 하나로 인상이 달라진다. (D1 확정 스펙 2.1)
- **오버레이 웨이트에 400·600을 쓰면 렌더가 멈춘다.** 번들은 500/700/800이고, 앱 CSS의
  `400 500` 매핑은 UI 텍스트용 편의이지 렌더러 계약이 아니다 — **화면에서는 정상으로 보이고
  렌더에서만 실패한다** (`font_path(400)`은 `AssetError`). (D2 확정 스펙 5.5)

반복해서 틀리는 자리:

- **색과 폰트 파일을 렌더러에 적지 말 것.** 프리셋은 `assets/`에 커밋돼 있고 조회는
  `shorts_maker.assets` 하나다(`sfx_path` / `caption_styles` / `background_presets` /
  `font_path`). 반대로 좌표·폰트 크기·요소별 `borderw`는 프리셋에 없다 — 렌더러의 표다.
  프리셋이 바꾸는 것은 색과 그림자뿐이라 교체해도 레이아웃이 회귀하지 않는다.
- **프리셋·형식 목록은 앱에 적지 않는다.** 자막 스타일·배경 프리셋·배경 파일 형식·오버레이
  후보가 전부 백엔드의 `presets` 메서드를 지난다 — 동결 배포에서 `assets/`는 백엔드 실행 파일
  옆이라 앱에서 본 경로와 다르고, 앱이 자기 목록을 들면 **화면에 보이는 목록과 렌더가 아는
  목록이 갈린다.** (#79, #80, #83)
- **자막 스타일·배경·배경 모션·볼륨은 장면 속성이 아니라 프로젝트 전역이다.** 모션은 특히
  장면별로 갈 수 없다 — `zoompan`에 timeline 지원이 없어 장면별 값은 표현식의 구간 분기가 되고,
  그것을 사람이 조정하게 하면 오버라이드 규칙이 두 벌 있는 자리가 늘어난다 (#34).
- **총 길이가 목표(45~60초) 아래인 것은 경고가 아니다.** 3문제 구성은 38초대가 정상 산출이라
  경고로 그리면 고칠 것이 없는 사용자에게 상시 뜬다. 경고 색은 60초 초과에만 쓴다.
- **사람이 `flagged`를 확인해도 `verify.status`·`confidence`를 쓰지 않는다.** 그 두 값은
  `quiz_verifier`와 `quiz_review.py`가 소유하므로, 사람 판단이 같은 칸을 쓰면 **다음 실행의
  임계값 판정이 조용히 통과한다.** 기록 위치는 `project.json`의 `review`다.
- **사람이 고친 장면 길이는 `scenes.json`에 쓸 수 없다.** `validate_scenes_final`이
  `duration < audio_duration`을 거부해 **저장이 실패하고 그 run 디렉터리가 다시 열리지
  않는다.** 자리는 `project.json`의 `render.scene_overrides`이고 키는 인덱스가 아니라
  `role` + `question_id`다. 길이·자막 문구·오버레이가 한 항목에 함께 얹히고 적용은
  `video_renderer.apply_scene_overrides` 하나다. (#82, #83)
- **장면 순서 변경은 문제 순서 변경이다** (#28). `hook`·`cta`는 위치가 고정이므로 속성 패널에
  순서 컨트롤을 다시 두면 같은 동작이 두 화면에 생긴다.

## 검증된 렌더링 사실

이 저장소에서 실제 렌더로 확인했다. 다시 조사하지 않아도 된다.

- **`drawtext`의 `fontsize`가 시간 표현식을 받고**(`T` 플래그) **그때 `text_w`·`text_h`가 매
  프레임 갱신된다.** 그래서 정답 확대에 PNG 시퀀스나 ASS가 필요 없고 `x=(500-text_w/2)` /
  `y=(1112-text_h/2)` 하나가 확대 내내 중심을 잡는다. `text_h`는 em 상자가 아니라 **잉크
  높이**다. 색은 표현식으로 바꿀 수 없어 등장색·강조색이 인스턴스 두 개이고, `borderw`도
  표현식을 받지 않아 확대 중 고정이다. (#22)
- **`zoompan`은 timeline을 지원하지 않고**(`enable` 옵션이 없다) **기본값 셋이 함정이다** —
  `s`가 `hd720`, `fps`가 25, `d`가 90이다. `s`를 빼면 배경이 1280x720으로 나와 오버레이 좌표가
  전부 어긋나고, `d`를 빼면 `-loop 1` 이미지에서 3초 주기로 같은 줌이 반복된다. **`on`은 출력
  프레임 번호이고 0부터 세며**(그래서 진행도를 프레임 수 - 1로 나눈다) `d=1`이 프레임 수를
  1:1로 유지한다. `x`·`y` 표현식의 `zoom`은 그 프레임에 대해 이미 계산된 배율이다. (#34)
- **배경 모션은 프레임마다 갱신되지 않는다.** 크롭 창이 정수 픽셀이라 **움직임의 실질 갱신률은
  fps가 아니라 강도가 정한다** — 기본값 0.08은 크롭 폭 1080 → 1000px이고 그 80단계가 영상
  전체에 퍼져, 40초에서 이웃한 60프레임 중 47쌍이 완전히 동일했다. 그래서 모션 테스트는 **떨어진
  두 프레임**을 비교하고, 이웃 프레임이 같은 것은 버그가 아니다. 반대로 정지 배경의 떨어진 두
  프레임을 바이트로 비교하면 **H.264 손실 압축 때문에 갈린다**(실측 평균 차이 0.005) — 차이는
  값으로 재야 한다. (#34)
- **`volume`은 timeline을 지원하지만 그것으로 ducking을 하면 클릭이 들린다.** `enable`로 켜고
  끄면 감쇠가 계단이라 전환 프레임에서 파형이 끊긴다 — 실측에서 전환 구간의 **샘플 간 최대
  변화량이 정상 구간의 8.6배**였다(램프는 1.24~1.35배, 최종 mp4에서 잰 값). 값 자체가 표현식을
  받으므로 사다리꼴을 적어 램프를 만드는데, **`eval`의 기본값이 `once`라 그것을 빼면 표현식이
  한 번만 평가되어 감쇠가 아예 걸리지 않는다.** 시각은 `t*fps`로 적어 `t*30--3` 같은 음수
  리터럴을 피한다. 클릭은 레벨이 아니라 파형의 불연속이라 **`astats`의 peak·RMS로는 드러나지
  않는다** — 재는 방법이 `max_sample_step`(wav 디코드 후 이웃 샘플 차)이다. (#35)
- **`drawbox`는 폭을 시간으로 움직일 수 없다.** 이 필터의 `t`는 시간이 아니라 **박스 두께**이고
  표현식에 시간·프레임 변수가 없어 `n`·`time`은 `Undefined constant`로 설정이 실패한다.
  그래서 카운트다운 진행 바는 **1초 단위 계단**이고 숫자도 초마다 `drawtext` 하나다. (#21)
- **`drawtext`의 `text`에 `%`가 있으면 그 요소가 아예 그려지지 않는다** — 일부가 잘리는 것이
  아니라 전체가 빈다. **`\%`로 이스케이프해도 막히지 않고 막는 것은 `expansion=none`
  하나다.** `:`는 인용부호 안에서도 옵션 구분자로 잘려 `\:`가 필요하고, `'`는 세 겹
  이스케이프다. 구현은 `overlay._escape`. (#20)
- **번인 텍스트는 줄 하나가 `drawtext` 하나다.** 여러 줄은 왼쪽 정렬로만 쌓이고 `text_w`가
  최장 줄의 폭이라 한 인스턴스에 `\n`을 넣으면 줄별 가운데 정렬이 나오지 않는다. 나누고 나면
  `LH` 값이 그대로 y 증분이라 **`line_spacing`을 쓰지 않는다.** `y`는 잉크 상단과 일치하고 한
  줄은 `y ~ y+fontsize`를 차지한다. (#20)
- **`enable` 구간은 초가 아니라 `n/fps` 나눗셈으로 적는다.** 반올림한 초를 적으면 요소가 한
  프레임 늦게 켜질 수 있다. `between(t,a,b)`는 양끝을 포함해 장면 경계에서 앞뒤 요소가
  겹치므로 `gte(t,a)*lt(t,b)`를 쓴다. (#20)
- **장면 경계의 프레임 정렬은 `video_renderer.align()` 하나가 소유한다.**
  `frames_i = round(duration_i * fps)` / `start_i = sum(frames_0..i-1) / fps`이고, 오버레이가
  시작 시각을 `duration`으로 다시 누적하면 요소마다 경계가 갈린다. 실측에서 프레임 정렬 총
  길이와 컨테이너 길이가 정확히 일치했다. (#19)
- **Pretendard의 한글 자폭은 0.86em이다**(36~132px, 웨이트 3종 공통). 줄바꿈 공식
  `floor(840/font_size)`는 1.0em 가정이라 줄이 컬럼의 85%에서 끝난다 — 넘칠 수 없는 쪽으로
  보수적이고 시안의 줄 수를 그대로 재현하므로 그대로 쓴다.
- **`gradients` 필터에는 `y1=1919`(높이-1)와 `speed=0`이 둘 다 필요하다.** `y1=1920`은 범위를
  벗어난 값이라 필터가 조용히 회전 기본 동작으로 되돌아가고, `speed`를 빼면 그라디언트가
  **시간에 따라 돌아** 50초 지점에서 위아래가 뒤집힌다. 둘 다 경고가 없고 후자는 한 프레임만
  보면 드러나지 않는다. (#38, #19)
- **`voice.mp3`는 무음 바닥 + `adelay` + `amix`로 만들고 길이가 요청값과 정확히 일치한다.**
  MP3 프레임 패딩이 길이를 밀지 않는 것을 확인했다(요청 10.000초 → 실측 10.000000초, 세그먼트
  시작도 1ms 안). 30fps 한 프레임 허용 오차를 걱정하지 않아도 된다. (#16)
- **`alimiter`는 기본값 두 개가 함정이다.** `level`의 기본값 `true`가 auto level이라 그냥 걸면
  최종 오디오가 0dB로 정규화된다(`level=disabled` 필수). `latency`를 빼면 attack(5ms)만큼
  **오디오 전체가 늦는다** — `latency=true`가 정확히 상쇄한다. (#23)
- **AAC 오버슈트는 소재에 따라 갈린다.** 리미터 출력이 정확히 -1.00 dBFS여도 AAC로 인코딩해
  디코드하면 톤에서 0.1dB, 광대역 노이즈에서 2.6dB를 넘는다 — 최종 mp4의 peak로 리미터를
  검증할 때 노이즈를 쓰면 인코더의 오버슈트를 재게 된다. (#23)
- **한국어 자막은 번들 폰트(`assets/fonts/Pretendard-*.otf`)로 정상 렌더된다.**
  `C:/Windows/Fonts/malgun.ttf`도 렌더는 되지만 **재배포가 금지되므로 쓰지 않는다.**
  `fontfile`에 Windows 경로를 줄 때 콜론을 이스케이프해야 한다 (`D\:/Git/...`).
- 개발 환경 FFmpeg 빌드에 libass와 `--enable-whisper`가 포함되어 있다. PRD 7.6의 word-level
  timestamp 개선에 별도 전사 의존성이 불필요할 수 있다 — 빌드 의존적이다. (#17, #33)

## 앱 경계

스택은 **Electron + React**, 백엔드는 앱이 자식으로 띄우는 **Python 프로세스 + stdio JSON
Lines**다(HTTP 서버가 아니다). 결정문은 PRD 14.1, 실측은 `docs/spikes/25-app-framework.md`.
아래는 실제로 밟은 함정이라 다시 조사하지 않아도 된다.

- **프리뷰 비용의 바닥은 필터가 아니라 FFmpeg 프로세스 기동이다.** 아무 일도 하지 않는
  `ffmpeg -version`이 이 머신에서 1.1~1.3초다. 그래서 **해상도를 낮춰도 빨라지지 않고**,
  프리뷰는 요청 장면 수와 무관하게 프로세스 하나로 전부 만든다 — 뒤쪽 장면 하나(2,049ms)와
  열한 장면 전부(2,247ms)가 거의 같아서 "닿은 장면만 다시 만든다"는 최적화가 198ms를 위해
  복잡도를 진다. 캐시는 지문으로 통째로 갈린다. (#25, #27)
- **Windows에서 파이프 stdio의 기본 인코딩은 cp949다.** `sys.stdout.reconfigure(encoding=
  "utf-8")`을 하지 않으면 한글이 든 응답이 UTF-8로 읽는 Node 쪽에서 **에러 없이 값만 깨진다.**
  자식 FFmpeg도, 스모크 픽스처 스크립트도 같은 함정을 지난다.
- **자식으로 띄운 FFmpeg는 백엔드의 stdin을 먹는다.** `stdin=subprocess.DEVNULL`을 주지 않으면
  ffmpeg가 프로토콜 줄을 가져가고 **실패도 로그도 없이 응답만 오지 않는다**(CLI에서는 멀쩡히
  끝나는 명령이다). ffmpeg를 새로 부르는 자리를 만들면 같이 준다.
- **최종 렌더 명령에서 `-map [audio]`만 빼면 프리뷰 명령이 실패한다** — `alimiter` 출력이
  연결되지 않는다. 오디오 체인을 필터 그래프에서 함께 들어내야 하고, `-c:v libx264`를 남긴 채
  `.png`로 쓰면 **경고 없이 H.264가 그 파일에 쓰인다.**
- **임시 파일은 확장자를 유지해야 한다** (`run_context.staging_path`). FFmpeg는 출력 형식을
  확장자로 정하므로 `voice.mp3.tmp-123`은 "Unable to find a suitable output format"으로 실패한다.
- **동결 배포(PyInstaller onedir)에서는 `assets/`가 실행 파일 옆에 있어야 하고**, 동적으로
  import되는 타입 패키지는 `--hidden-import`가 필요하다.
- **Vite는 `base: './'`가 필요하다.** `loadFile`로 여는 페이지라 절대 경로는 파일 시스템 루트를
  가리킨다. 번들 폰트는 `app/` 밖의 `assets/fonts/`를 CSS 상대 경로로 참조한다 — 앱 안으로
  복사하면 D1이 관리하는 폰트가 두 벌이 된다.
- **저장 여부를 main에 알리는 IPC만 동기다** (`sendSync` + `useLayoutEffect`). `invoke`로
  보내면 화면이 먼저 바뀌고 main이 나중에 알아, **그 틈에 창을 닫으면 확인 없이 닫힌다.**
- **백엔드 오류는 문자열이 아니라 `code` + `message` + `details`(+ 렌더 실패의 `raw`)다.**
  앱이 원인 종류에 따라 다른 것을 그리므로 메시지를 문자열 매칭하는 코드를 두면 문구를 다듬는
  순간 깨진다. 스키마 위반으로 열기에 실패해도 **이미 열려 있던 프로젝트는 그대로 둔다.**
- **`conftest`의 `stub_ffmpeg`는 `run`과 `Popen` 둘을 바꿔야 한다.** 하나만 바꾸면 렌더 경로에
  가짜 오디오가 진짜 FFmpeg로 간다 — 원인이 렌더처럼 보이지만 아니다. (#30)
- **렌더는 `Popen`이고 그 경로를 CLI도 함께 쓴다.** `run`이 해 주던 넷을 손으로 지킨다 —
  타임아웃은 진행 줄 루프가 아니라 `wait(timeout=…)`가 재고(루프에서 재면 멈춘 ffmpeg에 상한이
  없다), **stderr는 파이프가 아니라 임시 파일이다**(stdout만 읽는 동안 그 파이프가 차면
  ffmpeg가 거기서 막힌다). (#30)
- **렌더 중에 백엔드가 끝나면 ffmpeg가 남아 사용자가 앱을 닫은 뒤에 mp4를 완성한다.** 렌더
  스레드는 daemon이라 stdin EOF에서 그냥 사라진다 — `kill_active`를 `atexit`에 건다. (#30)
- **편집 잠금·동시 실행 판정은 화면이 아니다.** 컨트롤을 비활성으로 그리는 것만으로는 메뉴·
  단축키·스모크가 지나가므로 편집 잠금은 `App.patchProject` 하나가, 두 번째 렌더 거절(`busy`)은
  백엔드가 판정한다. (#30)
- **프리뷰 지문에서 빼는 기준은 "프레임에 닿지 않는 것"이다** (`PREVIEW_BLIND_SECTIONS`).
  빼지 않으면 확인 버튼 한 번이나 볼륨 슬라이더 한 칸이 프레임 전부를 다시 만들고 결과는 같은
  그림이다. 목록이 "닿는 것"이 아니라 **"닿지 않는 것"** 쪽인 이유는, 새 섹션이 아무것도 하지
  않아도 지문에 들어가야 화면이 옛 그림에 머물지 않기 때문이다. (#28, #81)
- **`review`를 두 목록만 골라 다시 조립하면 새 칸이 조용히 사라진다.** `patchReview`에 넘기는
  함수는 항상 `...current`로 시작한다 — 저장·재시작 뒤에 값이 없어지는 형태로 드러난다. (#82)
- **같은 오버라이드 규칙이 앱과 백엔드에 두 벌 있다** (`scenes.effectiveScenes` /
  `video_renderer.apply_scene_overrides`). 프리뷰는 왕복이라 방금 입력한 길이가 장면 목록과 총
  길이에 즉시 보이지 않으면 편집 도구가 되지 못한다. 규칙이 한 줄이고 키·필드 이름이 스키마에서
  오므로 갈릴 여지가 좁다 — **늘리지 말 것.**
- **오버라이드는 확정 검증 다음에 얹는다** (`render`·`preview` 두 자리가 같은 순서). 낭독보다
  짧은 `duration`은 `validate_scenes_final`이 거부하는 값이라 순서를 뒤집으면 사람이 줄인
  길이에서 렌더가 멈춘다. (#82)
- **`timeline`과 `captions`의 입구가 하나씩 더 있다** (`place_narration`, `build_applied`).
  이유는 같다 — 얹은 사본은 확정 검증이 거부하는 길이와 스키마가 모르는 `overlays` 키를 들고
  있어서, `captions.build`에 그대로 넘기면 사람이 줄인 길이에서 자막 생성이 실패한다. (#77)
- **오버라이드가 있으면 `scenes.json`의 `narration_offset`이 `voice.mp3`의 실제 배치와
  다르다.** 앞은 얹기 전(확정 검증을 통과해야 한다), 뒤는 얹은 뒤(렌더가 그리는 것이
  그쪽이다)다 — 버그로 보고 맞추려 들면 둘 중 하나가 계약을 어긴다. (#77)
  **음악 ducking 구간이 그 앞쪽 값에서 온다** (`audio_mix.narration_windows`) — 사람이 길이를
  고친 run에서 감쇠가 낭독과 어긋날 수 있고, 그것을 맞추려면 오버라이드 규칙을 아는 자리가
  세 번째로 생긴다. 그 상태를 표시하는 것은 앱의 `review.timeline_stale`이다. (#35)
- **재생성 뒤 화면이 옛 그림에 머물면 프리뷰 effect의 deps를 본다.** 재생성은 파일만 바꾸므로
  프로젝트만 보면 갱신되지 않는다. 세그먼트를 전부 재사용하는 재생성이 이 머신에서 5.2초이고
  그 대부분이 `ffprobe` 프로세스 기동이다. (#77)
- **앱 스모크는 앱을 여러 번 띄운다.** 한 프로세스 안에서 다시 여는 것은 "재시작하고 다시
  열었다"의 증거가 되지 못하고, 고아 프로세스 확인은 Electron만 `taskkill /F`(트리 아님)로
  죽여야 의미가 있다. 확인 대화상자는 `ask` 하나를 바꿔 끼운다.
- **스모크에서 상태 플래그와 원하는 값이 온 것은 다르다.** 직전 장면의 프레임이 서 있는
  화면도 `ready`이고, `open()`이 resolve된 것과 화면이 반영한 것도 다르며
  (`window.__smoke`는 렌더마다 다시 달린다), 프리뷰는 `pending === null`까지 봐야 같은 서명
  두 요청이 겹치지 않는다. 편집 직후 렌더를 부르면 **옛 프로젝트가 간다.**
- **앱 스모크의 재생성은 재합성이 없는 경로만 밟는다.** 백엔드 프로세스가 진짜 provider를
  만들어 재합성이 일어나면 **네트워크로 나가고**, 픽스처를 바꿔 끼울 자리가 없다. 합성이 실제로
  도는 경로는 `tests/test_regenerate.py`가 대역으로 지킨다. 순서 변경이 스모크에 없는 이유도
  같다 — 재사용 키가 장면 인덱스라 순서만 바꿔도 전부 다시 합성한다. (#77)
