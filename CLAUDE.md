# CLAUDE.md

YouTube Shorts Maker — 세로형 쇼츠 자동 생성 엔진 + 편집 앱.
기획은 `docs/PRD.md`와 `docs/types/quiz.md`, 작업 규칙은 `CONTRIBUTING.md`에 있다.
아래는 그 문서들을 읽어서는 알 수 없거나, 읽으면 오해하게 되는 것만 적는다.

## 작업 방식

모든 작업은 GitHub 이슈 단위로 진행한다. 착수 전 `CONTRIBUTING.md`를 읽는다 —
라벨 체계, 리파인먼트, 브랜치·PR 규칙이 거기 있다. 이슈 본문의 완료 조건이 완료 정의다.

미해결 결정사항은 PRD 14장과 퀴즈 스펙 10장에 모여 있다. 결정을 새로 내렸으면 코드만
바꾸지 말고 해당 장을 갱신한다.

## 로드맵

`src/shorts_maker/`와 `tests/`는 CLI 골격·설정·스키마·타입 레지스트리(Phase 0 완료)에
LLM provider 레이어(#48)와 퀴즈 생성기·검증기·검수 게이트(#9, #10, #11), 퀴즈 장면
템플릿(#12), 메타데이터 생성기(#13), TTS provider 레이어(#14), 세그먼트 합성(#15),
타임라인 확정(#16), 자막 생성(#17), 렌더 골격(#19), 번인 오버레이(#20), 카운트다운(#21),
정답 강조 애니메이션(#22), 효과음 믹싱(#23), 전 구간 스모크 테스트(#24)까지 있다. `app/`에는
Electron + React 셸과 stdio JSON Lines 백엔드(`api.py`)가 있고, 프로젝트를 열고 저장하는
왕복(#26) 위에 장면 목록·세로형 프리뷰·속성 패널 3분할(#27)과 **문제 편집 화면**(#28)이
올라와 있다. 속성 패널에는 **장면 길이 조정**(#82)이 들어왔고 남은 편집 컨트롤은 #79~#81·#83,
렌더 실행은 #30이 그 자리에 채운다.
CLI 한 번에 `verify`가 확정된 `quiz.json`, flagged 경고, `scenes.json`, `metadata.json`,
낭독 장면별 `audio/seg-*.mp3`, `voice.mp3`, `captions.srt`, `project.json`, 그리고 규격에 맞는
`final_short.mp4`까지 나온다 — **화면에는 hook·질문·카운트다운·정답 확대·해설·cta가 나오고
비프와 정답 효과음이 들린다.** `assets/`에는 효과음(#18)과
폰트·배경·자막 스타일 프리셋(#38)이 있고 조회는 `assets.py` 하나다 — `sfx_path()` /
`caption_styles()` / `background_presets()` / `font_path(weight)`. **Phase 3이 끝났고 수직
슬라이스가 완성됐다. Phase 4는 #28까지 들어갔고 다음은 #79다** — #25 결정(Electron + React,
stdio JSON Lines)과 D2 앱 UI 시안 1차 배치 수령이 그 앞의 게이트였다.
**#29는 구현 이슈가 아니라 추적 이슈다** — 리파인먼트에서 #79(자막 스타일·배경 프리셋),
#80(배경 사용자 파일), #81(볼륨), #82(장면 길이), #83(자막 텍스트·오버레이)으로 갈라졌고
**D2 2차 배치를 수령했고**(확정 스펙 7장) **#82가 들어갔다 — 다음은 #79다.**
2차 발주 범위는 자막 텍스트·오버레이 둘로 좁혀졌고 **S1은 발주하지 않았다** — #26이
토큰·인벤토리로 조립해 이미 동작한다.
**#82와 #83은 한 메커니즘을 공유한다** — 장면 길이·자막 문구·오버레이가 모두
`project.json`의 `render.scene_overrides` 한 항목(`role` + `question_id`)에 얹히고, #82가
그 섹션과 적용 지점(`video_renderer.apply_scene_overrides`)을 만들어 뒀다 (PRD 14.1).
**#28이 #77(앱에서 편집 반영 재생성)을 갈라냈다** — 앱은 편집을 콘텐츠 파일에 남기고
"낡았다"를 표시할 뿐이고, `scenes.json`·오디오·자막을 다시 만드는 것은 그쪽이다.
`scenes.json`은
`validate_scenes_final()`을 통과하는 확정 상태이므로 자막과 렌더러가 그대로 입력으로 받는다.
이슈 #1–#37이 아래 순서를 따르며 번호가 Phase 순서와 같다. **#38 이후는 나중에 추가된
이슈이므로 번호가 Phase 순서를 따르지 않는다** — 소속은 아래 표를 본다.

| Phase | 이슈 | 내용 |
| --- | --- | --- |
| 0 | #1–#8 | provider 결정, CLI·config·스키마·타입 플러그인 골격 |
| 1 | #48, #9–#13 | LLM provider 구현, 퀴즈 텍스트 산출물 |
| 2 | #14–#18 | 오디오와 자막 |
| 3 | #55, #56, #38, #19–#24 | 렌더링 — **#24 통과가 실질 MVP 완성** |
| 4 | #25–#30, #77, #79–#83 | 편집 앱 — #79~#83은 #29에서 갈라낸 편집 컨트롤 |
| 5 | #31–#37 | 범용 입력 경로, 품질 개선, 업로드 검토 |

```
#1(LLM) ─> #48 ─┬─> #9 ─> #10 ─> #12 ─┐
#7(스키마) ─────┘                      ├─> #15 ─> #16 ─> #17 ─┐
#2(TTS) ──> #14 ─────────────────────┘                        ├─> #20 ─> #21,#22 ─> #24
                        #55, #56, #38, #19 ───────────────────┘
```

**PRD 12장의 마일스톤 순서를 따르지 않는다.** PRD는 앱 골격(M2)을 렌더링(M6)보다 앞에 두지만,
편집 대상도 렌더 결과도 없는 상태에서 에디터를 만들면 재작업이 발생한다. 위 순서는 수직
슬라이스를 먼저 완성하고 앱을 붙인다. PRD 순서로 되돌리지 말 것.

## 문서를 그대로 읽으면 틀리는 지점

- **정답 검증은 블라인드 방식이다.** 퀴즈 스펙 5장의 "2차 LLM 검증"을 생성된 정답을 보여주고
  "맞습니까?"라고 묻는 식으로 구현하면, 같은 모델이 자기 출력에 동조해 검증 효과가 없다.
  정답을 감추고 질문만 다시 물어 독립 답변을 받아 비교한다. 구현은 `quiz_verifier.py`이고
  방식은 퀴즈 스펙 5.1, 실측은 `docs/spikes/10-verifier-detection.md`에 있다. (#10)
- **검증에서 실검출이 나오는 경로는 모호성 프로브다.** 블라인드 재답변은 *정답이 틀린 게
  아니라 질문이 모호한* 유형을 원리상 잡지 못한다 — 재답변 모델도 같은 근거로 같은 답을
  낸다. 정답을 감춘 채 "답이 하나로 정해지는가"만 묻는 호출을 따로 두고, 그 판정이 정답
  일치와 **독립적으로** `flagged`를 만든다. 실측에서 재답변이 놓친 문제를 이 프로브가
  잡았다. (#10)
- **`verify.status = "verified"`는 "정답이 맞다"가 아니다.** "검증 단계가 결함을 찾지
  못했다"는 뜻이고, `unverified`는 성공이 아니라 "검증이 완료되지 않았다"이다. 임계값을
  적용해 `verified`를 `flagged`로 내리는 것은 검증기가 아니라 `quiz_review.py`다 — 검증기는
  `confidence`를 산출만 한다. (#10, #11)
- **flagged는 실행을 멈추지 않는다.** 퀴즈 스펙 5장의 "최종 렌더링 전 경고"를 중단으로
  구현하면 검수 주체인 사람에게서 검수할 산출물을 빼앗는다. 기본은 경고 후 진행에 종료
  코드 0이고, 멈추는 것은 `--fail-on-flagged`를 지정한 쪽의 선택이다. (#11)
- **검수 훅은 세 번째 플러그인 축이 아니다.** `ShortsType.content_review`는 `config_check`와
  같은 성격의 훅이고, 교체 가능한 축은 여전히 생성기·장면 템플릿 둘뿐이다. 파이프라인이
  받는 `ContentIssue`에 `confidence` 같은 칸을 열면 공통 코드가 타입별 판정 기준을 알게
  된다 — 어느 부분 / 무엇 / 왜 세 문자열로 끝낸다. (#11)

#4에서 아래 세 가지가 문서에 명문화됐다. 요약만 남기고 근거·기준값은 문서를 본다.

- **duration은 실측 오디오 길이 + 패딩으로 확정한다.** 스펙의 초 단위 값은 목표치다.
  낭독이 없는 장면(`countdown`, 현 스펙의 `hook`·`cta`)만 고정 길이. → PRD 7.5.1
- **오디오는 장면별 세그먼트 + 합성 트랙 2종을 남긴다.** `voice.mp3` 단일 파일이 아니다.
  세그먼트 파일명은 장면 인덱스(`audio/seg-003.mp3`)로 매긴다. → PRD 7.5.2
- **`TTSProvider` 계약에 길이가 없다.** 텍스트 하나 → 오디오 파일 하나가 전부이고, 실측은
  공통 코드(`tts/speech.py`)가 `ffprobe`로 한다. provider 보고값을 쓰는 경로를 만들면
  #16이 흔들린다. 캐시도 같은 층에 있고 **run 디렉터리 밖**(`.cache/tts`)이다 — run마다 새
  디렉터리라 안에 두면 반복 실행이 매번 전부 재합성한다. 단어 타이밍의 `None`(기능 없음)과
  빈 목록(이번엔 없음)은 다른 값이다. (#14, PRD 7.5·7.5.2·14.1)
- **세그먼트 재사용은 캐시와 다른 층이다.** 캐시(`.cache/tts`)는 run 사이에 합성을 아끼고,
  `audio/segments.json`은 **같은 run 디렉터리 안에서** 사람이 교체한 세그먼트를 지킨다.
  그래서 재사용 경로는 기록된 길이를 믿지 않고 파일을 다시 잰다 — `SpeechSynthesizer.measure`
  로 재는 이유는 측정 경계가 하나여야 하기 때문이다. (#15, PRD 7.5.2)
- **`captions.max_chars_per_line`은 번인 줄바꿈이 아니다.** D1 확정 스펙 5장의 줄당 글자 수
  (질문 13~15자, 정답 10자, 해설 23자)는 `drawtext` 요소마다 폰트 크기가 달라서 갈리는
  값이고, 렌더러가 `floor(840 / font_size)`로 계산한다. 그 숫자를 config 키에 옮겨 적으면
  SRT와 번인이 한 값을 공유하게 되어 어느 쪽도 맞지 않는다. 두 층은 갈라져 있다. (#17, #20)
- **공통 파이프라인은 `scenes.json`만 읽는다.** TTS·자막·렌더러·메타데이터가 `quiz.json`을
  직접 열면 타입 경계 위반이며 두 번째 타입 추가가 막힌다. → PRD 7.4.1, 퀴즈 스펙 1.1
- **산출물 JSON의 필드명·열거값은 `src/shorts_maker/schemas/`가 확정한다.** 문서(퀴즈 스펙 3장,
  PRD 7.5.2·7.10)는 그 이름을 설명하는 쪽이므로 문서만 고치면 계약이 바뀌지 않는다.
  `scenes.json`은 스키마 하나 + 확정 검증(`validate_scenes_final`) 구조다 — 오디오 필드가
  없는 초안도 통과해야 하기 때문이다. (#7)
- **타입 선언은 타입 패키지가 소유한다.** `shorts_types.py`는 이름 → 선언 모듈 경로만 알고,
  생성기·장면 템플릿·산출물 조건은 `types/<name>/__init__.py`의 `SHORTS_TYPE`에 있다.
  `scenes.json`·`project.json`의 `type` 후보도 검증 시점에 레지스트리를 조회하므로, 타입을
  등록하면 스키마가 따라온다 — 스키마에 이름을 추가하지 말 것. (#8)
- **타입별 설정 제약은 `config.SPEC`이 아니라 `SHORTS_TYPE.config_check`에 있다.** 퀴즈의
  문제 수 3~5 같은 값은 설정 로더가 알 수 없는 타입의 규칙이다. 파이프라인이 run 디렉터리를
  만들기 전에 부르므로 범위 위반이 빈 run 디렉터리를 남기지 않는다. 교체 가능한 축은 여전히
  생성기·장면 템플릿 둘뿐이고 이것은 사전 점검이다. (#9)
- **`--json-schema`로 넘기는 JSON Schema는 산출물 스키마에서 파생한다.** `Rule.to_json_schema()`
  가 변환하고 `Object.without()`이 코드가 채우는 필드를 뺀다. 프롬프트 쪽에 필드 이름을 손으로
  적으면 계약이 두 곳에 생긴다. 검증은 여전히 `Rule.check`가 한다 — 파생 결과로 검증하지 말 것. (#9)
- **퀴즈 스펙 1.1의 경계는 `tests/test_type_boundary.py`가 강제한다.** `types/` 밖에서 타입
  패키지를 import하거나 `load_quiz`를 쓰거나 `quiz.json` 문자열을 들면 테스트가 깨진다.
  타입 전용 정보가 렌더에 필요하면 장면 템플릿이 `scenes.json` 필드로 옮겨 담는다. (#8)
- **산출물은 타입·입력 경로에 따라 다르다.** `script.txt`·`summary.json`·`source.json`은
  퀴즈 + `--topic` 경로에서 생성되지 않고, 없는 것이 실패가 아니다. → PRD 6.2 표
- **전 구간 스모크(`tests/test_e2e_smoke.py`)는 `conftest`의 `stub_tts`·`stub_ffmpeg`를
  쓰지 않는다.** `StubTTS`가 쓰는 `b"stub-audio"`는 오디오가 아니라 진짜 FFmpeg가 디코드하지
  못하고, `stub_ffmpeg`는 `ffprobe`까지 가짜 길이(2.5초)로 답해 duration 확정이 실측 경로를
  지나지 않는다. 그 파일의 `ToneTTS`가 문장 길이에 비례한 실제 오디오를 내고 대역은 LLM
  하나뿐이다. **파이프라인 실행은 모듈 스코프 픽스처의 3회뿐이다** — 테스트마다 돌리면
  1080x1920 인코딩이 그만큼 붙는다 (현재 렌더 3회에 약 25초). (#24)

## D1 영상 디자인

시안은 외부에서 받았고 원본은 claude.ai/design 프로젝트에 있지만, **구현 기준은
`docs/design/d1-video-design-spec.md`다.** 시안과 그 문서가 다르면 문서가 이긴다 — 시안은
CSS로 그려졌고 렌더는 FFmpeg `drawtext`라서 같은 숫자가 같은 그림을 만들지 않는 지점이 있고,
시안 자체의 계산 오류도 거기서 고쳤다. 발주서(`d1-video-design-brief.md`)는 무엇을 요청했는지의
기록이지 구현 기준이 아니다.

- **시안의 CSS `-webkit-text-stroke` 값을 `borderw`에 그대로 넣으면 두 배로 두꺼워진다.**
  CSS stroke는 글리프 경로 중심 기준이라 절반만 보이고, `borderw`는 전부 바깥이다.
  이 디자인은 외곽선이 지배적 요소라 이 하나로 인상이 달라진다. (확정 스펙 2.1, #20)
- **#20~#22 착수 전에 #55·#56이 먼저 들어가야 한다.** 시안이 `scenes.json`에 없는 문구
  (countdown·answer의 질문, hook의 kicker)를 요구하고 텍스트 길이 상한을 전제한다. 이걸
  건너뛰면 렌더러에 고정 문자열이 박혀 타입 경계가 깨진다. (완료)
- **렌더러는 config를 열지 않는다 — `project.json`만 읽는다.** 자막 스타일·폰트 경로·cta 문구도
  `render` 섹션을 지나며(#20이 필드를 열었다), 값을 정하는 경로가 둘이 되면 앱(#29)이 편집한
  프로젝트와 CLI 렌더가 갈린다. 새 렌더 설정을 추가할 때 `config.SPEC`만 여는 것은 절반이다 —
  `schemas/project.py`와 `project.build`가 함께 움직여야 렌더에 도달한다. **config의 섹션과
  `project.json`의 섹션이 같을 필요는 없다** — 해설 등장 시각은 `timing.caption_onset_sec`에서
  `render.caption_onset_sec`로 간다 (#22). config는 값의 성격으로, 프로젝트는 읽는 주체로
  나뉜다. (PRD 7.10)
- **색과 폰트 파일을 렌더러에 적지 말 것.** 확정 스펙 6장의 프리셋은 `assets/`에 커밋돼 있고
  (#38) 조회는 `shorts_maker.assets`의 `caption_styles()` / `background_presets()` /
  `font_path(weight)` / `sfx_path(name)`뿐이다. 확정 스펙 5장의 좌표·폰트 크기·요소별 `borderw`는 반대로 프리셋에
  없다 — 렌더러의 표다. 프리셋이 바꾸는 것은 색과 그림자뿐이라 교체해도 레이아웃이 회귀하지
  않는다.
- **확정 스펙 6.3의 "9조합 전부 7:1 이상"은 틀렸었다.** #38에서 실측해 고쳤다 — P3 오렌지
  강조색이 4.90~6.67:1이다. 성립하는 하한은 전 조합 3:1(대형 텍스트 AA)과 ◎ 기본 짝 4.5:1이며
  `tests/test_visual_assets.py`가 커밋된 색값에서 다시 계산한다. 대비가 부족하다고 판단되면
  색을 바꾸는 것이 아니라 그 절의 표를 먼저 볼 것.
- **고정 장면 길이가 시안에 맞춰 짧아졌다** — hook 2.5초 / `countdown_sec` 3 / cta 3.0초.
  PRD 7.5.1과 퀴즈 스펙 2장의 표가 갱신된 값이다.

## D2 앱 UI 디자인

D1과 같은 구조다. 시안은 claude.ai/design에서 받았고 **구현 기준은
`docs/design/d2-app-design-spec.md`다** — 시안과 다르면 문서가 이긴다. 발주서
(`d2-app-design-brief.md`)는 무엇을 요청했는지의 기록이지 구현 기준이 아니다.
아래는 시안이 저장소 값을 잘못 알고 있던 지점이라 시안을 직접 보면 틀린다.

- **시안의 자막 스타일 카드에 적힌 권장 배경 두 개가 서로 바뀌어 있다.** `neon_mint`는
  `purple_gradient`, `orange_card`는 `deep_teal_gradient`이고 확정값은
  `assets/caption-styles/presets.json`의 `background`다. 시안이 적은 조합은 D1 확정 스펙
  6.3에서 △(권장하지 않음)이며, 스타일을 고르면 배경이 따라 바뀌는 동작이 있어 그대로 옮기면
  사용자가 스타일 선택만으로 나쁜 조합에 들어간다.
- **사람이 `flagged`를 확인해도 `verify.status`·`confidence`를 쓰지 않는다.** 시안의
  "확인함으로 표시"는 `status`를 `verified`로, `confidence`를 `1`로 덮어쓰도록 그려져 있지만
  그 두 값은 `quiz_verifier`(#10)와 `quiz_review.py`(#11)가 소유한다. 사람 판단이 같은 칸을
  쓰면 **다음 실행의 임계값 판정이 조용히 통과한다.** 기록 위치는 #28에서
  `project.json`의 `review` 섹션으로 확정됐다 (PRD 14.1·7.10).
- **자막 스타일·배경·볼륨은 장면 속성이 아니라 프로젝트 전역이다.** 시안은 이 셋을 선택된
  장면의 역할 표시 바로 아래에 놓았다. 속성 패널은 장면 구획과 프로젝트 구획으로 나눈다.
- **프리뷰는 높이에 맞추고 폭을 `높이 x 9/16`으로 계산한다.** 시안의 620x348.75는 창 높이
  900일 때의 값이라 폭을 고정하면 최소 창(720)에서 잘린다.
- **`text/3`(`#6E7480`)은 비활성·기호 전용이다.** 11px `caption`에 쓰면 창 바탕 위 4.08:1 /
  카드 위 3.82:1로 소형 텍스트 AA에 미달한다. 읽는 11px 문구는 `text/2`(`#A2A8B4`).
- **총 길이가 목표(45~60초) 아래인 것은 경고가 아니다.** 3문제 구성은 38초대가 정상 산출이라
  시안의 조건(`total < 45`)을 그대로 쓰면 고칠 것이 없는 사용자에게 경고가 상시 뜬다.
  경고 색은 60초 초과에만 쓴다.
- **발주서 5.4의 "장면 순서 변경"은 이미 들어와 있다.** #28의 문제 순서 변경이 곧 장면 순서
  변경이고(`app/src/types/index.ts` — "순서가 곧 장면 순서다") `hook`·`cta`는 위치가 고정이다.
  속성 패널에 순서 컨트롤을 다시 두면 같은 동작이 두 화면에 생긴다. (#29 리파인먼트)
- **오버레이 웨이트에 400·600을 쓰면 렌더가 멈춘다.** 2차 시안이 "번들 Pretendard 3웨이트"라며
  `400 | 600 | 800`을 적었지만 번들은 **500 / 700 / 800**이다(`assets.FONT_FILES`).
  앱 CSS가 `400 500` / `600 700` 범위로 매핑하는 것은 UI 텍스트용 편의이고 렌더러 계약이
  아니다 — `font_path(400)`은 `AssetError`다. **화면에서는 정상으로 보이고 렌더에서만
  실패한다.** (확정 스펙 7.1)
- **`hook`과 `cta`에는 낭독이 없다.** 2차 시안이 장면별 낭독 길이를 hook 2.1 / cta 2.6으로
  적었지만 `narrate: true`는 `question`·`answer`뿐이고 그 둘은 고정 길이 장면이다. 낭독 길이가
  없으므로 **길이 미달 경고의 비교 대상 자체가 없다** — 시안대로면 존재하지 않는 낭독을 근거로
  경고가 뜬다. (확정 스펙 7.1)
- **사람이 고친 장면 길이는 `scenes.json`에 쓸 수 없다.** 시안이 그린 "경고하되 값은 받는"
  동작을 거기에 구현하면 `validate_scenes_final`이 `duration < audio_duration`을 거부해
  **저장이 실패하고 그 run 디렉터리가 다시 열리지 않는다.** 자리는 `project.json`의 `render`
  뿐이고 키는 인덱스가 아니라 `role` + `question_id`다 (PRD 14.1, #82).

## 검증된 렌더링 사실

이 저장소에서 실제 렌더로 확인했다. 다시 조사하지 않아도 된다.

- **`drawtext`의 `fontsize`가 시간 표현식을 받고**(`T` 플래그), **그때 `text_w`·`text_h`가 매
  프레임 갱신된다.** 그래서 정답 확대에 PNG 시퀀스나 ASS가 필요 없고, `x=(500-text_w/2)` /
  `y=(1112-text_h/2)` 하나가 확대 내내 중심을 잡는다 — 79→132px에서 잉크 중심이 1.5px 안에
  머물렀다. `text_h`는 em 상자가 아니라 **잉크 높이**다(132px에서 120). 색은 표현식으로 바꿀
  수 없어 등장색·강조색이 인스턴스 두 개이고, `borderw`도 표현식을 받지 않아 확대 중 고정이다.
  (#22, 확정 스펙 2.2)
- **`drawbox`는 폭을 시간으로 움직일 수 없다.** 이 필터의 `t`는 시간이 아니라 **박스 두께**이고
  (`t=5:w='t*100'` → 정확히 500px), 표현식에 시간·프레임 변수가 없어 `n`·`time`은
  `Undefined constant`로 설정이 실패한다. `drawtext`의 `fontsize`가 `t`를 받는 것과 다르다.
  그래서 카운트다운 진행 바는 **1초 단위 계단**이고 숫자도 `%{eif}` 하나가 아니라 초마다
  `drawtext` 하나다 — 다른 요소와 같은 `n/fps` 경계를 쓰기 위함이다. 연속 감소를 되살리는
  세 가지 방법과 각각의 비용은 확정 스펙 2.3에 적어 뒀다. (#21)
- **`voice.mp3`는 무음 바닥 + `adelay` + `amix`로 만들고, 길이가 요청값과 정확히 일치한다.**
  `anullsrc`에 `-t`로 총 길이를 주고 세그먼트를 `adelay`로 밀어 `amix=normalize=0`으로 얹는
  방식이다. **MP3 프레임 패딩이 길이를 밀지 않는지 확인했다** — 요청 10.000초에 실측
  10.000000초, 세그먼트 시작도 지정 오프셋과 1ms 안에서 일치했다. 30fps 한 프레임(0.033초)
  허용 오차를 걱정하지 않아도 된다. (#16, `timeline.mix_voice_track`)
- **`alimiter`는 기본값 두 개가 함정이다.** `level`의 기본값 `true`가 **auto level**이라 그냥
  걸면 최종 오디오가 0dB로 정규화되어 낭독 레벨이 달라지고(`level=disabled` 필수),
  `latency`를 빼면 attack(5ms)만큼 **오디오 전체가 늦는다** — `latency=true`가 정확히 상쇄한다
  (요청 1.000초 → 없이 1.005 / 있으면 1.000). `limit=0.891`(-1 dBFS)은 합이 +5 dBFS인 입력에서도
  지켜졌다. (#23)
- **`adelay` 배치 오차는 5ms 안이고, AAC 오버슈트는 소재에 따라 갈린다.** 리미터 출력이 정확히
  -1.00 dBFS여도 그것을 AAC로 인코딩해 디코드하면 톤에서 0.1dB, 광대역 노이즈에서 2.6dB를
  넘는다 — 최종 mp4의 peak로 리미터를 검증할 때 노이즈를 쓰면 인코더의 오버슈트를 재게 된다.
  같은 이유로 `sine` lavfi 소스의 출력은 **-18 dBFS**다(꽉 찬 신호가 아니다). (#23)
- **한국어 자막**은 번들 폰트(`assets/fonts/Pretendard-*.otf`, #38)로 정상 렌더된다.
  `C:/Windows/Fonts/malgun.ttf`도 렌더는 되지만 **재배포가 금지되므로 쓰지 않는다.**
  `fontfile`에 Windows 경로를 줄 때 콜론을 이스케이프해야 한다 (`D\:/Git/...`).
- **`drawtext`의 `text`에 `%`가 있으면 "Stray %" 경고와 함께 그 요소가 아예 그려지지 않는다.**
  일부가 잘리는 것이 아니라 전체가 빈다. **`\%`로 이스케이프해도 막히지 않고, 막는 것은
  `expansion=none` 하나다** (#20 실측). `:`는 인용부호 안에서도 옵션 구분자로 잘리므로 `\:`가
  필요하고, `'`는 세 겹 이스케이프를 쓴다. 규칙 전체와 검증 방법은 확정 스펙 7.4에 있고 구현은
  `overlay._escape`다. `%{eif}` 확장이 필요한 요소(#21의 카운트다운 숫자)는 자기 인스턴스에서
  기본 확장 모드를 쓰면 된다.
- **번인 텍스트는 줄 하나가 `drawtext` 하나다.** `drawtext`의 여러 줄은 왼쪽 정렬로만 쌓이고
  `text_w`가 최장 줄의 폭이라, 한 인스턴스에 `\n`을 넣으면 시안의 줄별 가운데 정렬이 나오지
  않는다. 나누고 나면 `LH` 값이 그대로 y 증분이 되므로 **`line_spacing`을 쓰지 않는다** —
  확정 스펙 2.4가 예고한 폰트 메트릭 역산이 필요 없어졌다. (#20)
- **`enable` 구간은 초가 아니라 `n/fps` 나눗셈으로 적는다.** `2.533`처럼 반올림한 초를 적으면
  반올림이 실제 프레임 시각보다 커졌을 때 요소가 한 프레임 늦게 켜진다. 또 `between(t,a,b)`는
  양끝을 포함해서 장면 경계 프레임에 앞뒤 요소가 겹치므로 `gte(t,a)*lt(t,b)`를 쓴다. (#20)
- **Pretendard의 한글 자폭은 0.86em이다** (36~132px, 웨이트 3종 공통). 줄바꿈 공식
  `floor(840/font_size)`는 1.0em 가정이라 줄이 컬럼의 85% 안에서 끝난다 — 넘칠 수 없는 쪽으로
  보수적이고 시안의 줄 수를 그대로 재현하므로 그대로 쓴다 (확정 스펙 7.3). `drawtext`의 `y`는
  잉크 상단과 일치하고 한 줄은 `y ~ y+fontsize`를 차지한다.
- **`gradients` 필터에는 `y1=1919`(높이-1)와 `speed=0`이 **둘 다** 필요하다.** `y1`에 1920을
  주면 범위를 벗어난 값이라 필터가 회전 기본 동작으로 조용히 되돌아가고 램프가 y703에서
  끝난다. `speed`를 빼면 기본값 0.01로 그라디언트가 **시간에 따라 돌아** 50초 지점에서
  위아래가 뒤집힌다 — `x0`~`y1`을 지정해도 무시되지 않는다. 둘 다 경고가 없고, 후자는
  한 프레임만 보면 드러나지 않는다. 명령과 실측은 확정 스펙 6.2에 있다. (#38, #19)
- **장면 경계의 프레임 정렬은 `video_renderer.align()` 하나가 소유한다.**
  `frames_i = round(duration_i * fps)` / `start_i = sum(frames_0..i-1) / fps`이고, 오버레이가
  `enable='between(t,a,b)'`에 쓸 시작 시각을 `duration`으로 다시 누적하면 요소마다 경계가
  갈린다. `Timeline.span()`이 돌려주는 끝 시각은 다음 장면 시작과 **같은 부동소수점 값**이다.
  실측 — 프레임 정렬 총 길이와 컨테이너 길이가 정확히 일치했고(11.733333초), `voice.mp3`의
  낭독 시작도 `narration_offset`과 1ms 안에서 맞았다. (#19, PRD 7.7)
- 개발 환경 FFmpeg 빌드에 libass와 `--enable-whisper`가 포함되어 있다. PRD 7.6의 word-level
  timestamp 개선에 별도 전사 의존성이 불필요할 수 있다. 빌드 의존적이므로 다른 환경에서는
  확인이 필요하다. (#17, #33)

## 앱 경계 (#25 결정, #26 구현)

스택은 **Electron + React**, 백엔드는 앱이 자식으로 띄우는 **Python 프로세스 + stdio JSON
Lines**다. 근거와 측정치는 `docs/spikes/25-app-framework.md`, 결정문은 PRD 14.1에 있다.
아래는 프로토타입에서 실제로 밟은 것들이라 다시 조사하지 않아도 된다.

- **프리뷰 비용의 바닥은 필터가 아니라 FFmpeg 프로세스 기동이다.** 아무 일도 하지 않는
  `ffmpeg -version`이 이 머신에서 1.1~1.3초다(같은 머신 `cmd /c ver`는 0.2초). 그래서 프리뷰
  1프레임이 0.8~2.4초이고 **해상도를 1/4로 낮춰도 빨라지지 않는다.** 프레임마다 프로세스를
  띄우는 프리뷰는 #27에서 기각된 상태로 시작한다. (#25, PRD 7.9)
- **Windows에서 파이프 stdio의 기본 인코딩은 cp949다.** 백엔드가
  `sys.stdout.reconfigure(encoding="utf-8")`을 하지 않으면 한글이 든 응답이 UTF-8로 읽는 Node
  쪽에서 **에러 없이 값만 깨진다.** 같은 이유로 자식 FFmpeg도 `encoding="utf-8"`로 연다.
- **자식으로 띄운 FFmpeg는 백엔드의 stdin을 먹는다.** ffmpeg가 stdin을 조작 입력으로 읽으므로
  `stdin=subprocess.DEVNULL`을 주지 않으면 프로토콜 줄을 가져가고 렌더가 끝나지 않는다.
  **#27에서 실제로 밟았다** — CLI에서는 멀쩡히 1.7초에 끝나는 명령이 앱에서만 끝나지 않고,
  실패도 로그도 없이 응답만 오지 않는다. 이제 `video_renderer`(렌더·프리뷰)·`timeline`·
  `overlay`의 ffmpeg 호출 네 군데가 전부 이것을 준다. ffmpeg를 새로 부르는 자리를 만들면
  같이 준다.
- **최종 렌더 명령에서 `-map [audio]`만 빼면 프리뷰 명령이 실패한다** — `alimiter` 출력이
  연결되지 않는다. 오디오 체인을 필터 그래프에서 함께 들어내야 하고, `-c:v libx264`를 남긴 채
  `.png`로 쓰면 **경고 없이 H.264가 그 파일에 쓰인다.**
- **동결 배포(PyInstaller onedir)에서는 `assets/`가 실행 파일 옆에 있어야 한다.**
  `assets.ASSETS_DIR`이 `shorts_maker/assets.py`에서 두 단계 위를 보기 때문이고, 없으면 어느
  경로를 찾았는지 말하며 실패한다. 동적으로 import되는 타입 패키지는 `--hidden-import`가 필요하다.

아래는 #26에서 실제로 밟은 것들이다.

- **저장 여부를 main에 알리는 IPC만 동기다** (`sendSync` + `useLayoutEffect`). `invoke`로
  보내면 화면이 먼저 바뀌고 main이 나중에 알아, **그 틈에 창을 닫으면 확인 없이 닫힌다** —
  스모크가 1ms 차이로 밟았다. main은 프로젝트 내용을 들지 않고 이 플래그 하나로 판단한다.
- **`run_context.write_artifact`는 원자적이지 않고, 그래도 된다.** 파이프라인은 매번 새 run
  디렉터리에 쓰므로 손상시킬 원본이 없다. 이미 있는 파일을 고쳐 쓰는 것은 앱의 저장뿐이고
  그쪽이 `api.write_atomically`(임시 파일 + `os.replace`)를 지난다. 직렬화 형식은
  `serialize_artifact` 하나여서 CLI가 쓴 파일을 앱이 저장만 해도 diff가 생기지 않는다.
- **백엔드 오류는 문자열이 아니라 `code` + `message` + `details`다.** 앱이 원인 종류에 따라
  다른 것을 그리므로(스키마 위반은 필드 목록, 파일 없음은 다른 디렉터리 안내) 메시지를
  문자열 매칭하는 코드를 두면 문구를 다듬는 순간 깨진다. 스키마 위반으로 열기에 실패해도
  **이미 열려 있던 프로젝트는 그대로 둔다.**
- **앱 스모크(`app/smoke/run.mjs`)는 앱을 세 번 띄운다.** 한 프로세스 안에서 다시 여는 것은
  "재시작하고 다시 열었다"의 증거가 되지 못하고, 고아 프로세스 확인은 Electron만
  `taskkill /F`(트리 아님)로 죽여야 의미가 있다. 확인 대화상자는 `ask` 하나를 바꿔 끼운다 —
  모달이 뜨면 자동 실행이 거기서 멈춘다.
- **Vite는 `base: './'`가 필요하다.** `loadFile`로 여는 페이지라 절대 경로(`/assets/...`)는
  파일 시스템 루트를 가리킨다. 번들 폰트는 `app/` 밖의 `assets/fonts/`를 CSS에서 상대 경로로
  참조해 빌드가 `dist/`로 복사한다 — 앱 안으로 복사하면 D1이 관리하는 폰트가 두 벌이 된다.

아래는 #27에서 실제로 밟은 것들이다.

- **프리뷰는 요청 장면 수와 무관하게 프로세스 하나다.** 필터는 마지막 요청 프레임까지 어차피
  돌기 때문에 **뒤쪽 장면 하나(2,049ms)와 열한 장면 전부(2,247ms)가 거의 같은 값이다.** 그래서
  "편집이 닿은 장면만 다시 만든다"는 최적화는 198ms를 위해 복잡도를 진다 — 매번 전부 만들고
  캐시가 지문으로 통째로 갈린다. 장면마다 프로세스를 띄우면 19,131ms다(절대값은 실행마다
  ±20% 흔들리고 걸려 있는 것은 비율이다). 실측은
  `docs/spikes/27-preview-frames.md`.
- **`useContentSize: true`가 없으면 확정 수치가 그 창 폭에서 나오지 않는다.** BrowserWindow의
  기본 `width`는 창틀을 포함하므로 Windows에서 콘텐츠가 1440보다 좁아지고, 패널 폭이
  `100vw`(콘텐츠 폭) 위에 있어 296/340 대신 292/335가 된다. 확정 스펙 3.1의 1440x900·1280x720은
  **콘텐츠 크기**로 읽는다.
- **스모크에서 `data-state`가 `ready`인 것과 원하는 프레임이 온 것은 다르다.** 직전 장면의
  프레임이 서 있는 화면도 `ready`라 `select(n)` 직후 `ready`를 기다리면 즉시 통과한다.
  프레임 인덱스로 기다린다. 같은 이유로 `open()`이 resolve된 것과 화면이 그것을 반영한 것도
  다르다 — `window.__smoke`는 렌더마다 다시 달린다.
- **대기 표현 2종은 값을 고쳐야 관찰된다.** 장면 이동은 캐시 적중이라 3~5ms이고, React가
  중간 커밋을 건너뛰어 `loading` 상태가 DOM에 나타나지 않을 수 있다. 값을 고치면 캐시가
  통째로 비어 2초대 대기가 생기고, 그 대기 중에 다른 장면으로 옮기면 두 표현이 차례로
  화면에 선다 — 스모크가 그 순서로 밟는다.

아래는 #28에서 실제로 밟은 것들이다.

- **앱 백엔드는 콘텐츠 산출물의 이름을 알 수 없다.** `api.py`는 `types/` 밖이라
  `tests/test_type_boundary.py`가 `load_quiz` import도 `"quiz.json"` 문자열도 막는다. 그래서
  `ShortsType`에 `content_schema`가 생겼고 `content_artifact`는 그 파생값(`schema.name`)이다 —
  파일명과 검증이 한 값에서 나온다. `config_check`·`content_review`와 같은 성격의 선언이고
  **교체 가능한 축은 여전히 생성기·장면 템플릿 둘뿐이다.**
- **`project.json`에 렌더러가 읽지 않는 섹션이 처음 생겼다** (`review`). 그래서 프리뷰 지문이
  프로젝트 전체가 아니라 `APP_STATE_SECTIONS`를 뺀 것이다 — 빼지 않으면 확인 버튼 한 번이
  프레임 전부를 다시 만들고 결과는 같은 그림이다. 목록이 "렌더가 읽는 것"이 아니라 **"렌더가
  읽지 않는 것"** 쪽인 이유는, 새 섹션이 아무것도 하지 않아도 지문에 들어가야 화면이 옛
  그림에 머물지 않기 때문이다. 섹션 자체는 **선택**이다 — 필수로 만들면 이 필드가 생기기 전에
  만들어진 run 디렉터리가 열리지 않는다.
- **문제를 지운 자리에 `max(id) + 1`로 새 번호를 주면 장면이 참조하는 번호를 가져간다.**
  스모크가 밟았다 — 3번을 지우고 추가하면 다시 3번이 되고, `scenes.json`의 옛 장면 셋이 새
  문제의 것으로 읽힌다(배지도 재생성 표시도 번호를 따라간다). 그래서 `add`가 **장면이 아직
  참조하는 번호까지 받아** 상한을 잡는다. 재생성(#77)이 장면을 다시 만들면 그때 풀린다.
- **`stale`은 되돌려도 떨어지지 않는다.** `scenes.json`이 어느 문구에서 만들어졌는지 기록해
  두는 곳이 없어 비교 기준이 "직전 값"뿐이다. 낡지 않은 것을 낡았다고 말하는 쪽이 반대보다
  안전하다. 반면 **장면 순서·개수가 낡았는지는 저장하지 않는다** — 콘텐츠의 번호 나열과
  `scenes.json`의 나열을 비교하면 나오고, 그래서 재시작 뒤에도 같은 답이 나온다.
- **셸은 콘텐츠의 필드를 모른다.** `quiz.json`의 필드를 아는 앱 코드는 `app/src/types/`에
  등록된 모듈뿐이고 셸은 `ContentItem` 다섯 칸만 본다. **어느 필드가 낭독으로 가는지도
  타입의 지식이다** — 셸은 `narration` 문자열이 달라졌는지만 보고 확인 해제와 `stale`을 함께
  건다. 난이도·카운트다운을 고쳐도 오디오는 그대로이므로 그 둘은 들어가지 않는다.
- **문제 편집 화면에는 프리뷰가 없다**(확정 스펙 3.2의 2분할). 콘텐츠를 고쳐도 `scenes.json`이
  그대로라 그림이 바뀌지 않기 때문이고, 그래서 이 화면의 스모크는 **FFmpeg를 요구하지
  않는다.**

아래는 #82에서 실제로 밟은 것들이다.

- **`review`를 두 목록만 골라 다시 조립하면 새 칸이 사라진다.** `editContent`와 `removeItem`이
  `{acknowledged, stale}`을 새로 만들고 있어서 `timeline_stale`이 **저장·재시작 뒤에 조용히
  없어졌다** — 스모크가 밟았다. 앱의 전제는 "프로젝트를 통째로 오가므로 스키마가 늘어도 값을
  잃지 않는다"인데 그 전제가 콜백 안에서 깨져 있었다. `patchReview`에 넘기는 함수는 항상
  `...current`로 시작한다.
- **오버라이드는 확정 검증 **다음**에 얹는다.** 낭독보다 짧은 `duration`은
  `validate_scenes_final`이 거부하는 값이라(그래서 `project.json`에 산다) 순서를 뒤집으면
  사람이 줄인 길이에서 렌더가 멈춘다. `render`와 `preview` 두 자리가 같은 순서를 쓴다.
- **같은 규칙이 앱과 백엔드에 두 벌 있다** (`scenes.effectiveScenes` /
  `video_renderer.apply_scene_overrides`). 프리뷰는 왕복이라 방금 입력한 길이가 장면 목록과
  총 길이에 즉시 보이지 않으면 편집 도구가 되지 못한다. 규칙이 "키가 맞는 장면의 값을 갈아
  끼운다" 한 줄이고 키·필드 이름이 스키마에서 오므로 갈릴 여지가 좁다 — 늘리지 말 것.
- **스모크 픽스처에 낭독 장면이 없었다.** `app/smoke/make_run.py`가 `narrate`를 붙이지 않아
  낭독 미달 경고 경로가 관찰되지 않았다. 이제 `question`·`answer`가 낭독 장면이고
  **세그먼트 mp3는 만들지 않는다** — 확정 검증은 파일명이 장면 인덱스와 맞는지만 보고, 앱과
  프리뷰는 오디오를 열지 않는다.
