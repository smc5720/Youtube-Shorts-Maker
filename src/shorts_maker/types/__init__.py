"""타입 전용 콘텐츠 생성기와 장면 템플릿 (PRD 11장).

**이 패키지 밖의 모듈은 여기를 import하지 않는다.** 타입으로 가는 유일한 길은
`shorts_types` 레지스트리이고, 공통 파이프라인(TTS·자막·렌더러·메타데이터)은 타입 전용
산출물을 열지 않는다 (퀴즈 스펙 1.1 / PRD 7.4.1). `tests/test_type_boundary.py`가 강제한다.

반대 방향은 열려 있다 — 타입 패키지는 `schemas`와 `config`를 자유롭게 쓴다.
"""
