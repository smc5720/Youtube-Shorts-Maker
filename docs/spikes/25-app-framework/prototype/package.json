{
  "name": "spike-25-prototype",
  "private": true,
  "version": "0.0.0",
  "description": "이슈 #25 검증용 프로토타입 — React → Electron main → Python 백엔드. 제품 코드가 아니다.",
  "main": "electron/main.js",
  "scripts": {
    "build": "vite build",
    "start": "npm run build && electron .",
    "smoke": "npm run build && electron . --smoke"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "electron": "^43.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^8.0.0"
  }
}
